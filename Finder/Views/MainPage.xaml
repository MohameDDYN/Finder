<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Finder.Views.MainPage"
             Title="Finder"
             BackgroundColor="{StaticResource BackgroundColor}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="⚙" Clicked="OnSettingsToolbarClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout Padding="16,12" Spacing="0">

            <!-- ── Status Card ────────────────────────────────────────── -->
            <Frame Style="{StaticResource CardFrameStyle}" Margin="0,0,0,12">
                <StackLayout Spacing="6" HorizontalOptions="Center">

                    <Label Text="📍" FontSize="36" HorizontalOptions="Center" />

                    <Label Text="{Binding StatusText}"
                           TextColor="{Binding StatusColor}"
                           FontSize="15"
                           FontAttributes="Bold"
                           HorizontalOptions="Center" />

                    <!-- Last update time -->
                    <Label Text="{Binding LastUpdateText}"
                           TextColor="{StaticResource TextLightColor}"
                           FontSize="12"
                           HorizontalOptions="Center" />
                </StackLayout>
            </Frame>

            <!-- ── Tracking Controls Card ─────────────────────────────── -->
            <Frame Style="{StaticResource CardFrameStyle}" Margin="0,0,0,12">
                <StackLayout Spacing="6">

                    <Label Text="TRACKING CONTROLS"
                           Style="{StaticResource SectionLabelStyle}"
                           Margin="0,0,0,6" />

                    <!--
                        FIX: Removed IsEnabled="{Binding IsServiceRunning, Converter=...}"
                        from Start button. Command.CanExecute is the ONLY mechanism
                        controlling IsEnabled. Having both caused Xamarin.Forms to
                        let CanExecuteChanged override the binding, permanently
                        disabling the Stop button when IsBusy was briefly true.
                    -->
                    <Button x:Name="btnStart"
                            Text="▶  Start Tracking"
                            Style="{StaticResource SuccessButtonStyle}"
                            Command="{Binding StartServiceCommand}" />

                    <!--
                        FIX: Removed IsEnabled="{Binding IsServiceRunning}" from
                        Stop button. IsEnabled is now controlled exclusively by
                        StopServiceCommand.CanExecute = () => IsServiceRunning.
                    -->
                    <Button x:Name="btnStop"
                            Text="■  Stop Tracking"
                            Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding StopServiceCommand}" />

                    <!-- Refresh status -->
                    <Button Text="↻  Refresh Status"
                            Style="{StaticResource PrimaryButtonStyle}"
                            BackgroundColor="{StaticResource PrimaryLightColor}"
                            Command="{Binding RefreshStatusCommand}" />

                </StackLayout>
            </Frame>

            <!-- ── Actions Card ───────────────────────────────────────── -->
            <Frame Style="{StaticResource CardFrameStyle}" Margin="0,0,0,12">
                <StackLayout Spacing="6">

                    <Label Text="ACTIONS"
                           Style="{StaticResource SectionLabelStyle}"
                           Margin="0,0,0,6" />

                    <!-- Share location button -->
                    <Button Text="📤  Share Current Location"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding ShareLocationCommand}" />

                    <!-- View history button -->
                    <Button Text="📅  Location History"
                            Style="{StaticResource PrimaryButtonStyle}"
                            BackgroundColor="{StaticResource TextSecondaryColor}"
                            Command="{Binding ViewHistoryCommand}" />

                </StackLayout>
            </Frame>

            <!-- ── Activity indicator ────────────────────────────────── -->
            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{StaticResource PrimaryColor}"
                               Margin="0,8" />

            <!-- ── Footer ─────────────────────────────────────────────── -->
            <Label Text="Finder tracks your location and sends&#10;updates to your Telegram bot."
                   TextColor="{StaticResource TextLightColor}"
                   FontSize="12"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   Margin="0,16,0,8" />

        </StackLayout>
    </ScrollView>
</ContentPage>